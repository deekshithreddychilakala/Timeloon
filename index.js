/**
 * Entrypoint for React Native / Expo native builds
 *
 * Some native build tools expect an `index.js` at the project root. This
 * file delegates to Expo's AppEntry so the native runner can find the
 * application bootstrap reliably.
 */

import 'expo/AppEntry';
